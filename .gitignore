# Paramètres locaux de l'utilisateur (ne pas publier)
parametres.json

# LICENCE — secrets du vendeur : NE JAMAIS partager / distribuer / publier
outils/cle_privee.pem
outils/cle_publique.txt
outils/licences.json
licence.key
licence.key*
admin-prive/
licences_generees.txt

# Données locales / journaux (ne pas publier)
calendrier_utilisateur.json
*.log

# Build PyInstaller (les .spec SONT suivis : nécessaires pour builder, sans secret)
build/
dist/

# Python
__pycache__/
*.pyc
.venv/
venv/

# Contenus locaux (médias à poster)
contenus/
sources/
planning/
scratch_uniq/
